package com.github.y120.bukkit.questlog.quest;

public enum ObjectiveType {
	APPROACH,	// Approach an {x, y, z}.
	KILL,		// Kill {n} of some entity {e}.
	COLLECT,	// Collect {n} of some item {i}.
	HOLD,		// Stay within an {x, y, z} for {t} seconds.
}
